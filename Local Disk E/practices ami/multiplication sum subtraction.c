#include <stdio.h>
int main ()
{
int x, y, sum, multiplication ,subtraction;
x= 5;
y=10;
sum = x+y;
printf ("%d+%d= %d" );

multiplication = x*y;
printf ("result of multiplication: %d\n", multiplication);

subtraction = x-y;
printf ("result of subtraction: %d\n", subtraction);
return 0;
}
